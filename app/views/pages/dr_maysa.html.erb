<%# Dr. Maysa — the founder. NeuSkin Clinic (standalone) design port. English / LTR. %>

<%# ---- Full-bleed portrait hero, quote overlaid (1:1 port of design §isMaysa) ---- %>
<section class="over-hero over-hero--maysa" data-controller="reveal parallax">
  <div class="over-hero-media" data-parallax data-parallax-speed="0.08">
    <%= sec_image_tag("dr_maysa", "drmaysa_hero", :maysa_hero, alt: "Dr. Maysa Al-Rashid", class: "kb") %>
  </div>
  <div class="over-hero-copy over-hero-copy--wide">
    <div class="mysa-hero-eyebrow reveal" data-reveal-target="item"><%= sec_text("dr_maysa", "drmaysa_hero", "eyebrow").presence || "The Founder · Medical Director" %></div>
    <h1 class="mysa-hero-quote" data-reveal="up"><%= sec_text("dr_maysa", "drmaysa_hero", "quote").presence || "“I would rather treat one thing well than ten things to fill a calendar.”" %></h1>
    <div class="mysa-hero-sign" data-reveal="up" data-reveal-delay="160"><%= sec_text("dr_maysa", "drmaysa_hero", "sign").presence || "— Dr. Maysa Al-Rashid" %></div>
  </div>
</section>

<%# ---- Credentials line (thin band, 1:1 port of design §isMaysa) ---- %>
<section class="creds-band" data-controller="reveal">
  <div class="creds-row reveal" data-reveal-target="item">
    <div><%= sec_text("dr_maysa", "drmaysa_creds", "cred_1").presence || "MBBS · Dermatology" %></div>
    <div><%= sec_text("dr_maysa", "drmaysa_creds", "cred_2").presence || "Aesthetic Medicine Fellowship" %></div>
    <div><%= sec_text("dr_maysa", "drmaysa_creds", "cred_3").presence || "Regenerative Aesthetics" %></div>
    <div><%= sec_text("dr_maysa", "drmaysa_creds", "cred_4").presence || "SFDA / MOH Licensed" %></div>
    <div><%= sec_text("dr_maysa", "drmaysa_creds", "cred_5").presence || "Member · Saudi Society of Dermatology" %></div>
  </div>
</section>

<%# ---- How I think about treatment ---- %>
<section class="section section--surface" data-controller="reveal">
  <div class="section-inner" style="max-width:780px;margin-inline:auto">
    <span class="eyebrow reveal" data-reveal-target="item"><%= sec_text("dr_maysa", "drmaysa_philosophy", "eyebrow").presence || "How I think about treatment" %></span>
    <div class="body-copy reveal d1" data-reveal-target="item">
      <p><%= sec_text("dr_maysa", "drmaysa_philosophy", "body_1").presence || "I trained in dermatology expecting to spend my life treating disease. What I found, in practice, was something quieter and more interesting — women who did not need fixing, only reading. The face in front of me was rarely the problem. The plan around it usually was." %></p>
      <blockquote class="pull-quote reveal" data-reveal-target="item"><%= sec_text("dr_maysa", "drmaysa_philosophy", "pull_1").presence || "“Skin is data. My job is to read it before anyone touches it.”" %></blockquote>
      <p><%= sec_text("dr_maysa", "drmaysa_philosophy", "body_2").presence || "So I built NeuSkin around the assessment, not the treatment. Every patient begins by being read — skin, hair, body — as a baseline, and only then is anything proposed. The plan is written down, sequenced, and reviewed every quarter against where we began." %></p>
      <p><%= sec_text("dr_maysa", "drmaysa_philosophy", "body_3").presence || "There are things I will not do. I will not chase a trend onto a healthy face. I will not perform a procedure a patient does not understand. And I will not sell a year of sessions when three honest months would do." %></p>
      <blockquote class="pull-quote reveal" data-reveal-target="item"><%= sec_text("dr_maysa", "drmaysa_philosophy", "pull_2").presence || "“The best result I can give you is the one no one can name.”" %></blockquote>
      <p><%= sec_text("dr_maysa", "drmaysa_philosophy", "body_4").presence || "What I insist on is measurement. We document where you begin and we compare honestly as we go, so progress is never left to memory or to flattery. That is the whole method, and it is the only promise I make." %></p>
    </div>
    <p class="sign center reveal d2" data-reveal-target="item" style="text-align:center"><%= sec_text("dr_maysa", "drmaysa_philosophy", "sign").presence || "— Maysa" %></p>
  </div>
</section>

<%# ---- The Maysa Method™ — three steps (light contrast band) ---- %>
<section class="section section--light mysa-method orn-host" data-controller="reveal">
  <%= render "shared/orn", variant: "petal", pos: "tr" %>
  <div class="section-inner">
    <div class="mysa-method-eyebrow reveal" data-reveal-target="item"><%= sec_text("dr_maysa", "drmaysa_method", "eyebrow").presence || "The Maysa Method™" %></div>
    <h2 class="mysa-method-title reveal" data-reveal-target="item"><%= sec_text("dr_maysa", "drmaysa_method", "title").presence || "Three steps. One connected system." %></h2>
    <%# Expanding panel gallery: steps side by side; the active step is wide
        with its copy shown and image zoomed — hovering a neighbour hands the
        expansion over. Texts and per-card images stay dashboard-editable. %>
    <% mysa_defaults = ["site/space/device-room.webp", "site/people/maysa-founder.webp", "site/space/clinic-3.webp"] %>
    <% mysa_steps = [["Diagnostic mapping", "Skin, hair and body baseline — captured with imaging and a doctor-led examination."],
                     ["The 12-month plan",  "A written roadmap across skin, hair, body and lifestyle — yours to keep."],
                     ["Quarterly review",   "We measure, compare and adjust as your skin responds over time."]] %>
    <div class="ns-expand reveal d1" data-reveal-target="item">
      <% mysa_steps.each_with_index do |(title_default, body_default), idx| %>
        <% i = idx + 1 %>
        <div class="ns-panel">
          <img src="<%= sec_card_image("dr_maysa", "drmaysa_method", i, mysa_defaults[idx]) %>" alt="">
          <div class="ns-panel-copy">
            <span class="num"><%= sec_text("dr_maysa", "drmaysa_method", "step_#{i}_n").presence || format("%02d", i) %> — <%= t("treatments.step", default: "Step") %></span>
            <h3><%= sec_text("dr_maysa", "drmaysa_method", "step_#{i}_title").presence || title_default %></h3>
            <p><%= sec_text("dr_maysa", "drmaysa_method", "step_#{i}_body").presence || body_default %></p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="mysa-method-cta reveal d2" data-reveal-target="item">
      <%= link_to (sec_text("dr_maysa", "drmaysa_method", "cta_label").presence || "Request the Maysa Method™ Assessment"), inquire_path(codeword: "METHOD"), class: "btn btn--rose" %>
    </div>
  </div>
</section>

<%# ---- In her words — three clips ---- %>
<section class="section section--surface" data-controller="reveal">
  <div class="section-inner">
    <span class="eyebrow reveal" data-reveal-target="item"><%= sec_text("dr_maysa", "drmaysa_clips", "eyebrow").presence || "In her words" %></span>
    <div class="clips reveal d1" data-reveal-target="item" style="margin-top:var(--space-block)">
      <div class="clip">
        <% clips_imgs = sec_gallery_urls("dr_maysa", "drmaysa_clips", [:doctor_coat, :consult, :beauty_eyes]) %>
        <%= image_tag clips_imgs[0], alt: "What is worth your money?" %>
        <span class="play">▶</span>
        <span class="ctitle"><%= sec_text("dr_maysa", "drmaysa_clips", "clip_1_title").presence || "What is worth your money?" %></span>
      </div>
      <div class="clip">
        <%= image_tag clips_imgs[1], alt: "How I say no to a patient" %>
        <span class="play">▶</span>
        <span class="ctitle"><%= sec_text("dr_maysa", "drmaysa_clips", "clip_2_title").presence || "How I say no to a patient" %></span>
      </div>
      <div class="clip">
        <%= image_tag clips_imgs[2], alt: "What I'd tell my best friend" %>
        <span class="play">▶</span>
        <span class="ctitle"><%= sec_text("dr_maysa", "drmaysa_clips", "clip_3_title").presence || "What I'd tell my best friend" %></span>
      </div>
    </div>
  </div>
</section>

<%# ---- Closing CTA ---- %>
<section class="section" data-controller="reveal">
  <div class="section-inner">
    <div class="cta-center orn-host reveal" data-reveal-target="item">
      <%= render "shared/orn", variant: "rings", pos: "halo", size: 380 %>
      <h2><%= sec_text("dr_maysa", "drmaysa_cta", "title_1").presence || "Request a private consultation with" %> <span class="em"><%= sec_text("dr_maysa", "drmaysa_cta", "title_em").presence || "Dr. Maysa." %></span></h2>
      <p><%= sec_text("dr_maysa", "drmaysa_cta", "body").presence || "All initial consultations are conducted personally by Dr. Maysa." %></p>
      <%= link_to (sec_text("dr_maysa", "drmaysa_cta", "cta_label").presence || "Begin a private inquiry"), inquire_path, class: "btn" %>
    </div>
  </div>
</section>
