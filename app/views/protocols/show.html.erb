<%# Protocol detail — NeuSkin Clinic (standalone) design port. English / LTR. %>
<% p = @protocol %>

<%# Hero image: map by slug like the _card partial, fallback treatment_room. %>
<% proto_hero_imgs = { "maysa-method" => "site/protocols/maysa-method.jpg", "90-day-glow-reset" => "site/protocols/glow-reset.jpg",
                       "brides-180" => "site/protocols/bridal.jpg", "reset-crown" => "site/protocols/hair.jpg",
                       "8-week-sculpt" => "site/protocols/sculpt.jpg", "skin-insider" => "site/protocols/insider.jpg" } %>
<% hero_img = proto_hero_imgs[p.slug] %>
<% for_who = p.who_for.to_s.split(/[.\n]/).first.to_s.strip.presence || "By assessment" %>

<%# 1 · Split hero ---- %>
<section class="split-hero" data-controller="reveal parallax">
  <div class="split-hero-media reveal" data-reveal-target="item" data-parallax data-parallax-speed="0.08">
    <% if hero_img %>
      <%= image_tag hero_img, alt: p.name, class: "kb" %>
    <% else %>
      <%= ns_image_tag :treatment_room, alt: p.name, class: "kb" %>
    <% end %>
  </div>
  <div class="split-hero-copy">
    <span class="eyebrow reveal" data-reveal-target="item"><%= p.meta %></span>
    <h1><span data-reveal="mask" data-reveal-delay="120"><span><%= p.name %><% if p.trademark %><span class="tm">™</span><% end %></span></span></h1>
    <p class="lead reveal d2" data-reveal-target="item"><%= p.promise %></p>
    <ul class="specs reveal d3" data-reveal-target="item">
      <li><span class="k">Duration</span><span class="v"><%= p.duration %></span></li>
      <li><span class="k">Format</span><span class="v">Doctor-led · personalised</span></li>
      <li><span class="k">For</span><span class="v"><%= for_who %></span></li>
    </ul>
    <p class="reveal d4" data-reveal-target="item"><%= link_to "Request your plan", inquire_path(codeword: p.codeword, persona: p.persona), class: "btn" %></p>
  </div>
</section>

<%# 2 · Who it's for ---- %>
<% signs = [
     ["01", "You want to know what your skin actually needs — measured, not guessed."],
     ["02", "You'd rather follow one sequenced plan than collect single treatments."],
     ["03", "You value discretion, and a doctor who will tell you what to skip."]
   ] %>
<section class="section section--surface" data-controller="reveal">
  <div class="section-inner split">
    <div class="split-text reveal" data-reveal-target="item">
      <span class="eyebrow">Who it's for</span>
      <p class="lead"><%= p.who_for %></p>
    </div>
    <div class="split-text reveal d1" data-reveal-target="item">
      <ul class="rows">
        <% signs.each do |n, t| %>
          <li><span class="n"><%= n %></span><span class="t"><%= t %></span></li>
        <% end %>
      </ul>
      <p class="sign" style="font-style:italic;margin-top:24px">This protocol is not for everyone — and that is intentional.</p>
    </div>
  </div>
</section>

<%# 3 · What's inside (staged timeline) ---- %>
<section class="section" data-controller="reveal">
  <div class="section-inner">
    <div class="sec-head reveal" data-reveal-target="item">
      <span class="eyebrow">What's inside</span>
      <h2><%= p.scope %></h2>
    </div>
    <div class="stages reveal d1" data-reveal-target="item">
      <% p.stages.each do |stage| %>
        <div class="stage">
          <span class="wk"><%= p.loc(stage, "week") %></span>
          <h3><%= p.loc(stage, "title") %></h3>
          <p><%= p.loc(stage, "body") %></p>
        </div>
      <% end %>
    </div>
    <p class="note reveal" data-reveal-target="item" style="margin-top:46px">Final protocol scope is confirmed during your Maysa Method™ assessment. All treatments are personalised.</p>
  </div>
</section>

<%# 4 · What's not included ---- %>
<% exclusions = p.excludes.to_s.split(/[.;\n]/).map(&:strip).reject(&:blank?) %>
<% exclusions = [p.excludes.to_s.strip] if exclusions.empty? %>
<section class="section section--surface" data-controller="reveal">
  <div class="section-inner split">
    <div class="split-text reveal" data-reveal-target="item">
      <span class="eyebrow">What's not included</span>
      <h2>Honesty, set early.</h2>
    </div>
    <div class="split-text reveal d1" data-reveal-target="item">
      <ul class="exclude">
        <% exclusions.each do |item| %>
          <li><%= item %></li>
        <% end %>
      </ul>
    </div>
  </div>
</section>

<%# 5 · The patient journey ---- %>
<section class="section" data-controller="reveal">
  <div class="section-inner">
    <div class="sec-head reveal" data-reveal-target="item">
      <span class="eyebrow">The patient journey</span>
    </div>
    <div class="journey reveal d1" data-reveal-target="item">
      <div class="step"><span class="n">01</span><h3>Inquire</h3><p>A private message, reviewed personally.</p></div>
      <div class="step"><span class="n">02</span><h3>Consult</h3><p>A doctor-led diagnostic assessment.</p></div>
      <div class="step"><span class="n">03</span><h3>Plan</h3><p>A written, sequenced roadmap.</p></div>
      <div class="step"><span class="n">04</span><h3>Treat</h3><p>Sessions, reviewed each quarter.</p></div>
    </div>
  </div>
</section>

<%# 6 · One consented patient story ---- %>
<% if p.patient_story.present? && p.patient_story["quote_en"].present? %>
  <section class="section section--surface" data-controller="reveal">
    <div class="section-inner">
      <div class="pull-band reveal" data-reveal-target="item">
        <blockquote><%= p.patient_story["quote_en"] %></blockquote>
        <p class="by">— <%= p.patient_story["initials"] %> · <%= p.name %> · <%= p.patient_story["year"] %></p>
      </div>
    </div>
  </section>
<% end %>

<%# 7 · Frequently asked ---- %>
<% if p.faqs.present? %>
  <section class="section" data-controller="reveal">
    <div class="section-inner">
      <div class="sec-head reveal" data-reveal-target="item">
        <span class="eyebrow">Frequently asked</span>
      </div>
      <div class="faq-group reveal d1" data-reveal-target="item">
        <% p.faqs.each do |faq| %>
          <details class="faq">
            <summary><%= p.loc(faq, "q") %> <span class="sign">+</span></summary>
            <div class="faq-a"><%= p.loc(faq, "a") %></div>
          </details>
        <% end %>
      </div>
    </div>
  </section>
<% end %>

<%# 8 · Final CTA + WhatsApp codeword ---- %>
<section class="cta-center" data-controller="reveal">
  <h2 class="reveal" data-reveal-target="item">Begin <%= p.name %>.</h2>
  <p class="reveal d1" data-reveal-target="item">Send the codeword <strong><%= p.codeword %></strong> on WhatsApp for a private inquiry.</p>
  <p class="reveal d2" data-reveal-target="item">
    <a class="btn" href="<%= whatsapp_url(codeword: p.codeword) %>" target="_blank" rel="noopener">Send "<%= p.codeword %>"</a>
  </p>
</section>
