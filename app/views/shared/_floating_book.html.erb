<%# Floating booking affordance — fades in after the hero (booking_controller);
    leans toward the pointer on hover (magnetic_controller). %>
<%= link_to t("layout.floating_book"), inquire_path, class: "floating-book", data: { controller: "booking magnetic" } %>
